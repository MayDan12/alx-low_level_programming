#include <stdio.h>
/**
 * main - print digit of base 10
 * Return: 0 always (excellent)
 */
int main(void)
{
int may;
may = 0;
while (may < 10)
{
putchar(may + '0');
may++
}
putchar('\n');
return (0);
}
