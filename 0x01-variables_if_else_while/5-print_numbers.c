#include <stdio.h>
/**
 * main - print all single digit numbers
 * Return: 0 always (excellent)
 */
int main(void)
{
int dan;
for (dan = 0; dan < 10; dan++)
	printf("%d", dan);
putchar('\n');
return (0);
}
