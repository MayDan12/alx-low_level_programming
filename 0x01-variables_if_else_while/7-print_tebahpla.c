#include <stdio.h>
/**
 * main - reverse alphabet
 * Return: 0 always (excellent)
 */
int main(void)
{
char MD;

MD = 'z';
while
	(MD >= 'a')
	{
		putchar(MD);
		MD++;
	}
putchar('\n');
return (0);
}
