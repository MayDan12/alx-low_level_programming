#include "main.h"
/**
 * main - this prints the nanme of the program
 * @argc: number of argument passed
 * @argv: the array of argument
 * written by daniel
 * Return: o aiways
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
