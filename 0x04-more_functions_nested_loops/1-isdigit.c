#include "main.h"

/**
 * _isdigit - functions that checks for digit
 * written by daniel oluwanimotele
 * @c: is a good bot
 * Return: 0 and 1 always
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
